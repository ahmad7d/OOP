/**
 * The VoidRenderer class represents a renderer that does not display any information.
 * It implements the Renderer interface.
 */
public class VoidRenderer implements Renderer {

    /**
     * This method will be overridden by another class to provide specific rendering behavior.
     *
     * @param board The game board to be rendered.
     */
    @Override
    public void renderBoard(Board board) {
    }
}
